// 顶部菜单位置
export enum TopMenuPositionEnum {
    // 左
    LEFT='left',
    // 居中
    CENTER='center',
    // 右
    RIGHT='right'
}

// 页面切换动画
export enum PageAnimationEnum {

}

// 菜单模式
export enum MenuModeEnum{
    // 侧边
    SIDE,
    // 混合侧边
    MIX_SIDE,
    // 顶部
    TOP
}

// 主题模式
export enum ThemeModeEnum{
    // 明亮
    LIGHT,
    // 暗黑
    DARK
}